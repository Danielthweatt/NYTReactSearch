//Dependencies
const router = require('express').Router();
const articlesController = require('../../controllers/articlesController');

//Article Routes (to be written...)
router.route('').get(articlesController.something);

module.exports = router;
